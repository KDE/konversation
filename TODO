Large pastes: Let the user decide if they want an Editor or paste immediately
Make Config Dialogs fit into one Tab-Dialog and throw out single windows
Background color of Nicklist View
Pasting into text views should trigger the paste mechanism
Make colors for nick list configurable via GUI
<lizard> ich will nen shortcut haben das wenn ich irgendwo ssh eingebe das mit ne shell aufgeht ;)
Let the user decide how many quick buttons they want.
Think about another way to get rid of the cut off "Custom" text in color combos ...
Autojoin/User interaction on invite
URL catcher user interface
User interface for dialog options (don't show again dialogs)
Border changes on-the-fly using some kind of "redraw-pointer-list"
CHECK: Replace "0.0.0.0" with QString::null in dccTransfer to allow other than IPv4
Session management
Make font for quick buttons configurable
Different charsets / encodings for each channel / tab
Fortunes
Queries should get a Whois menu item
"notices" tab for all notices that are no channel notices
"snotices" tab for all server notices (prefix without "!" )
Indicate hilight on tab LED / label
DCC port range
cycle command
Move Encoding into identity
Make separate Highlight lists for each identity
Nick button in status pane does nothing
Measure away time and make it available via placeholder

<mETz[]> Eisfuchs: And why have the "Mitschnitt" button there? having it in the menubar is enough, no?

-privmsg (/msg) should be logged to the specific log file (if i do "/msg foo bar" i want to have this in konversation-foo.log)

nobs: "nette mirc- Features im normalen text-Fenster:  und  das markieren
      des letzten Zeitpunkts wo das Fenster aktiv war"
*nobs* noch ein Feature-Request: alle "technischen" Meldungen aus # und query ausblenden können;
       entweder ins Status-Fenster umleiten oder ganz verwerfen optional

Consistency check in Preferences:
- Command Char must be present

"/part" may close Queries, too.

Make %k (Channel Key Wildcard) work ... Seems to be a bit complicated

Show away messages only once per nick
Indicate own away status
Try to fix the disconnect / no reconnect problem
*nobs* noch ein Wunsch: die Reiter auf Wunsch an die Seite
Automatically gzip files before DCC SEND
Compute md5sum over DCCs and send / display the hash after transfer
Import notify list from .ircrc
Notify with more than 500 characters
Logrotate with timer or at shutdown, compress rotated logs
Stats for server lag, words per session per person / channel, quits etc.

*nobs* feature-request:   in der History suchen können, so wie in der Bash mit ctrl-r vielleicht

*nobs* feature request: Fenster die beim Schliessen offen waren, sollten beim Starten auch wieder offen sein (nicks online)
*Eisfuchs* Wie wäre es mit einem [X] öffne Nicks Online Fenster beim Start ?
*nobs* hm, oder so
*nobs* andockbar wäre vielleicht nett, so als Reiter wie die channels; vielleicht könnte man sich auch zusätzliche Infos merken, wie "last spoken" "last seen"
